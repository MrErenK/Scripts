# Sample config.env
# This is a sample configuration file for the kranul-build.sh script.

# Device Configuration
DeviceCodename="begonia"               # e.g., "begonia"
DeviceModel="Redmi Note 8 Pro"         # e.g., "Redmi Note 8 Pro"
DeviceArch="arm64"                     # Architecture (usually arm64)
DefConfig="begonia_user_defconfig"     # e.g., "begonia_user_defconfig"

# Kernel Configuration
KernelName="Astera"                    # e.g., "Astera
KERNEL_VARIANT="Neutron"               # e.g., "Neutron"

# Compiler Configuration
ClangName="neutron"                    # Options: azure, neutron, proton, zyc, aosp, yuki
CrossCompileFlag64="aarch64-linux-gnu-"
CrossCompileFlag32="arm-linux-gnueabi-"
CrossCompileFlagTriple="aarch64-linux-gnu-"

# AnyKernel Configuration
AnyKernelRepo="https://github.com/Neebe3289/AnyKernel3"
AnyKernelBranch="begonia"

# KernelSU options - Set one of these to "yes" (not both)
KERNELSU="no"                         # Set to "yes" to include KernelSU stable
KERNELSU_NEXT="no"                    # Set to "yes" to include KernelSU-next

# Build Options
GenBuildLog="yes"                     # Generate build.log
RegenerateDefconfig="no"              # Regenerate defconfig after build
CLEANUP="no"                          # Clean up after build
SEND_ANNOUNCEMENT="no"                # Send announcement to Telegram channel